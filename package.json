{
  "name": "c1-browser-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "./scripts/setup-emsdk.sh",
    "build": "make web",
    "test": "make test",
    "dev": "node scripts/serve.mjs",
    "start": "npm run build && npm run dev",
    "extract": "node scripts/extract-disc.mjs",
    "clean": "make clean"
  },
  "engines": {
    "node": ">=20"
  }
}
